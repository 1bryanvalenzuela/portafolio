---
const { type, id, placeholder } = Astro.props;
---

<label
  for={id}
  class="flex rounded-md border bg-white border-gray-200 shadow-sm focus-within:border-green-600 focus-within:ring-1 focus-within:ring-green-600"
>
  <input
    type={type}
    id={id}
    class="w-full peer ubuntu-regular border-none bg-transparent focus:border-transparent focus:outline-none focus:ring-0 px-4"
    placeholder={placeholder}
  />
</label>
